version: "3.9"

services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - PORTFOLIO_URL=https://afrehner.com
    ports:
      - "3001:3001"
    volumes:
      - reports_data:/app/reports

volumes:
  reports_data:
